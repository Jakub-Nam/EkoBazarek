import { NoUpperCaseDirective } from './no-upper-case.directive';

describe('NoUpperCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new NoUpperCaseDirective();
    expect(directive).toBeTruthy();
  });
});
